tabs()
	.{$prefix}-tabs
		width 100%
		margin-bottom 24px

		&.fullwidth
			.ui-tab-header-item
				flex-grow: 1

		&.raised
			border: none
			box-shadow: 0 0 2px alpha(black, 0.12), 0 2px 2px alpha(black, 0.2)

			.{$prefix}-tabs-body
				border: none

	.{$prefix}-tabs-header
		position: relative
		width: 100%
			
		.{$prefix}-tab-header-item .ui-ripple-ink .ripple.held
				opacity: 0.7

	.{$prefix}-tabs-header-items
		position: relative
		display: flex
		list-style: none
		margin: 0
		padding: 0

	// .{$prefix}-tabs-body
	// 	background-color: white
	// 	border-radius: 0
	// 	border: 1px solid $clr-grey-200
	// 	border-top: 0
	// 	padding: 16px
	// 	margin: 0

	.{$prefix}-tab
		outline: none
	// tab header
	.{$prefix}-tab-header-item {
		font-family: $font-stack;
		position: relative;

		display: flex;
		height: 48px;
		padding: 0 12px;
		text-transform: uppercase;

		align-items: center;
		justify-content: center;

		cursor: pointer;
		min-width: 80px;

		&.type-icon-and-text {
			display: flex;
			flex-direction: column;
			height: 72px;

			.{$prefix}-tab-header-item-icon {
				margin-bottom: 4px;
				margin-bottom: 4px;
			}
		}

		&.disabled {
			opacity: 0.4;
			cursor: default;
		}
	}

	// .{$prefix}-tab-header-item-text {
	// 	@extends $truncate-text;
	// }

	// .{$prefix}-tab-header-item-icon {
	// 	width: 24px;
	// 	height: 24px;
	// 
	// 	.{$prefix}-icon {
	// 		cursor: inherit;
	// 	}
	// }
	
	.{$prefix}-tabs-indicator
		position: absolute
		height: 2px
		bottom: 0
		left: 0
		right: 0
		
		transform: scale(0)
		transform-origin: left center
		transition: transform
		// @apply(--paper-tabs-selection-bar);
	
		&.align-bottom
			top: 0
			bottom: auto
			
		&.expand
			transition-duration: 0.075s
			transition-timing-function: cubic-bezier(0.4, 0.0, 1, 1)

		&.contract
			transition-duration: 0.09s
			transition-timing-function: cubic-bezier(0.0, 0.0, 0.2, 1)
			
// styling/theming mixing
tabs-style(background-color = $clr-grey-200, color = $clr-secondary-text-light, active-color = $clr-primary, indicator-color = $clr-primary)
	.{$prefix}-tabs-header
		background-color: background-color
		.{$prefix}-tabs-header-items
			color: color
			.{$prefix}-tab-header-item-icon
				color: color
		.{$prefix}-tab-header-item
			body[modality="keyboard"] &:focus
				outline: 1px solid active-color
		.{$prefix}-tab-header-item.active
			color: active-color
			.{$prefix}-tab-header-item-icon
				color: active-color	
	.{$prefix}-tabs-indicator
		background-color: indicator-color
	
