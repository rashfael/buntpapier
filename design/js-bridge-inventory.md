# JavaScript bridge inventory

Audited 2026-09-18 against `3.0.0-alpha.18`. The colour derivation has mostly moved to CSS. The remaining bridges handle keyword dispatch, contrast correction, geometry and observation. This inventory records retirement decisions; implementation remains planned where noted below.

Support below comes from the linked platform documentation, checked on the audit date. Baseline describes current browser releases; it does not prove support at our minimum versions (Chrome 147, Firefox 147, Safari 26). Removal requires checks at those versions and regression tests in all three engines. No new browser experiment ran for this inventory. The September 2026 spike found working inheritance and stylesheet-declared transitions in Chromium 147 and Firefox 148, but missed unset values and hidden-subtree changes. Public-token registration changes fallback semantics; observer adoption remains undecided, with WebKit and integration behavior unverified.

## Decisions

“Retire” means schedule removal with the stated replacement and checks. “Retire when Baseline” also requires the feature at our browser floor. “Keep” means the current contract still needs the code; any narrower exit condition is recorded in the row.

| Bridge and source | What JavaScript supplies | Native replacement and support | Decision |
|---|---|---|---|
| Former HSL decomposition and filled-label contrast, now [button.sass](../src/styles/components/button.sass), [checkbox.sass](../src/styles/components/checkbox.sass), [derived.sass](../src/styles/derived.sass) | Previously split colours into channels and chose light/dark ink | Relative colours, `color-mix()`, `light-dark()` and `contrast-color()` already implement this in our Sass. [Relative colours](https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Colors/Using_relative_colors); [contrast-color()](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/color_value/contrast-color) is Baseline April 2026. | Already retired. Correct the old JS explanation in the public guide. The internal API guide and current project instructions describe the replacement architecture. |
| `ensureReadable()`, blending, parsing and OKLCH conversion in [colors.ts](../src/utils/colors.ts); callers in button and checkbox | Adjusts accent ink against the local surface; handles translucent accents and browser colour serialization | Current `contrast-color()` chooses black or white. The [CSS Color 6 draft](https://drafts.csswg.org/css-color-6/) adds candidates and target contrast, but it is not a shipping replacement for our lightness search. Relative colour arithmetic alone cannot check a contrast ratio. | Keep. Retire when a native target-contrast solution reaches our floor and reproduces the required result. Do not delete the parser or `color-blend` while this caller remains. Infrastructure work raises the button text threshold from 3 to 4.5 and handles forced colours; checkbox graphics retain their separate contrast requirement. |
| Exported `deriveDarkVariant()` in [colors.ts](../src/utils/colors.ts), [index.ts](../src/index.ts) | Returns a hex string with adjusted OKLCH lightness/chroma for theme generation | Relative `oklch(from …)` with `max()`/`min()` can express the channel adjustments in CSS, already available in the CSS feature set we use. CSS cannot return a hex string to an application. | Keep the public JS utility. Document a CSS alternative in the API guide; removing the export needs an API migration, even after the ink guard goes. |
| Keyword-to-class callbacks in button, checkbox, input, select, both date pickers and progress-circular | Maps size, shape, weight, layout and icon placement to selectors | [`if(style(...): …)`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/if) can select property values on the element itself; still limited availability. [Container style queries](https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Containment/Container_size_and_style_queries) target descendants of the queried container. | Retire when `if()` is Baseline and supported at our floor. Keep classes now. Avoid adding wrapper elements solely to enable style queries. SVG radius calculations remain a separate dependency. |
| `--checkbox-icon` read in [checkbox.vue](../src/components/checkbox.vue) | Converts an arbitrary MDI icon name into a font class | CSS cannot concatenate a string into a DOM class or look up the MDI name-to-glyph mapping. `if()` would only enumerate a fixed set. | Keep. A CSS image/mask token could replace this API, but that is an icon contract change for API design. This read survives removal of the variant classes. |
| Registered colour mirrors in [derived.sass](../src/styles/derived.sass) | Gives JS resolved colours for each element instead of inherited token streams | `@property` is the native mechanism already in use. The declarations on `*` preserve local `color-scheme` resolution. | Keep while JS consumes colours. Remove only unused registrations after their last reader disappears; do not register public colour tokens as a shortcut. |
| [computedStyle.ts](../src/computedStyle.ts), [themeWatcher.ts](../src/themeWatcher.ts), [requestAnimationFrameMuxxer.ts](../src/requestAnimationFrameMuxxer.ts) | Reads CSS, publishes classes/styles, watches root theme changes and optionally polls every frame | [`transition-behavior: allow-discrete`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/transition-behavior) is Baseline August 2024, but transition events are not a complete computed-style observer. | Keep pending the observer decision. Its spike found unset-value and hidden-element gaps; WebKit and the integration prototype remain untested. Shrinking the observed set does not settle the observer choice. |
| [input-outline.ts](../src/utils/input-outline.ts), [text-metrics.ts](../src/utils/text-metrics.ts), radius maps and outline watchers in the four field components | Measures label text, constructs rounded SVG paths and opens the select outline where the menu joins | Native borders and intrinsic label layout can replace the drawing after a markup/design change. `field-sizing` sizes the control, while `anchor-size()` sizes/positions anchored boxes; neither supplies an SVG notch. | Keep for the current appearance. Prototype a CSS outline during the field contract work before expanding the field family, then decide whether to retire the geometry before adding controls. Details below. |
| Floating UI, trigger width measurement, dropdown height/offset maths and copied theme in [select.vue](../src/components/select.vue) | Places and sizes a teleported menu and restores part of its lost inherited theme | CSS anchor positioning, `anchor-size()`, position fallbacks and an in-place top-layer popover. [`anchor-size()`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/anchor-size) is Baseline January 2026. | Retire with the overlay rewrite. Keep the popup in its original DOM ancestry so tokens inherit. Verify available-height clipping, nested scrollers, viewport edges and the joined outline; basic anchor support alone does not prove equivalent layout. |
| Popper placement and coordinate-based Web Animations in [tooltip.ts](../src/directives/tooltip.ts) | Positions the tooltip and animates between calculated viewport coordinates | [CSS anchor positioning](https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Anchor_positioning/Using), a top-layer popover, and CSS opacity/translate transitions | Retire geometry and coordinate animation with the overlay rewrite after placement/fallback tests at our floor. Keep JS for hover delay, focus, Escape, accessibility relationships and open-state synchronization. |
| Thumb geometry, scroll/resize/mutation observers and drag handlers in [scrollbars.vue](../src/components/scrollbars.vue) and [scrollbar.ts](../src/directives/scrollbar.ts) | Reimplements native scrollbar rendering and interaction | Native overflow with [`scrollbar-width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scrollbar-width) (Baseline December 2024) and [`scrollbar-color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scrollbar-color) (Baseline December 2025) | Retire custom thumbs after a native-scrollbar migration check. Native bars will not reproduce the exact 6px-to-12px hover expansion. Preserve axis selection and documented events; check reverse flex, RTL, keyboard, touch and forced colours. Infrastructure work owns the check; it must explicitly accept the appearance change before removing either implementation. |
| Pointer coordinates and radius in [ripple-ink.ts](../src/directives/ripple-ink.ts) | Starts a ripple at the press location and covers the furthest corner | CSS handles animation, but has no established cross-browser value for an arbitrary pointer event's local coordinates. `:active` can drive a centred effect with different behavior. | Keep for the pointer-origin effect. Removing the effect is a design choice, not a Baseline milestone. Pointer cancellation, keyboard behavior and reduced motion belong to infrastructure work. |
| Visual state classes in input, select, checkbox and date pickers | Duplicates focus, checked, disabled, expanded and validation state for selectors | Native pseudo-classes, ARIA selectors and [`:has()`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Selectors/:has) (Baseline 2023) can style states present in descendant controls. | Retire redundant classes during field wiring and the select overlay rewrite. Keep state needed for behavior; `floating-label` also depends on model/placeholder content, and async validation cannot be inferred from `:user-invalid` alone. |

## Outline decision

Keep the existing outline until a replacement demonstrates the same required field behavior. The bridge measures labels using a hardcoded 12px font and constructs a pixel path from the box dimensions. It also couples the select outline to the menu's flipped placement. Replacing only the text measurement leaves most of that geometry intact.

[`field-sizing: content`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/field-sizing) became Baseline in June 2026, later than our stated minimum-browser baseline. It makes controls follow their content, not their external label. [`anchor-size()`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/anchor-size) is restricted to sizing, inset and margin properties on anchor-positioned elements; it cannot be substituted into `stroke-dasharray` or an SVG path.

The field-contract prototype should compare an ordinary border with a label above it against a notch built from intrinsic label layout and border segments. A real [`fieldset`/`legend`](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/fieldset) supplies a native notch but also group semantics; using it for decoration needs an accessibility check. Painting the surface colour behind the label is insufficient on translucent or patterned backgrounds.

Acceptance cases: no label, long and translated labels, custom and late-loading fonts, compact/inline layouts, all shapes, 200%/400% zoom, RTL, forced colours, and select opening above/below. If the joined-outline effect is dropped with the overlay rewrite, remove that branch with the select rewrite. Do not expand the SVG helper to new field types before this decision.

## Code that stays JavaScript

Date parsing, segmented editing, calendar generation, selection, validation, focus management, async button state and Vue model/events implement behavior. CSS does not replace them. Native popovers reduce overlay plumbing but still need synchronization with Vue and the accessibility contract.

`getIconClass()` maps the behavioral `icon` prop to the current icon provider. `resize-observer.ts` delivers resize callbacks and remains needed by the outline today; retain the directive's public contract independently of internal callers. `use-mobile.ts` has no callers in `src` in this audit. Pure presentation should use a media query; changing interaction based on pointer capabilities can still require `matchMedia()`.

The observer does not disappear merely because `if()` ships. Checkbox icon lookup and accent contrast still read CSS, and `deriveDarkVariant()` keeps part of the colour utility code alive independently of observation. Recheck actual imports and public exports before removing any helper or dependency.
